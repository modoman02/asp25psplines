% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_all_funcs.R
\name{run_all_funcs}
\alias{run_all_funcs}
\title{High-level fit for Gaussian location–scale P-splines}
\usage{
run_all_funcs(
  x,
  y,
  m = 20,
  l = 3,
  equi = TRUE,
  tp = FALSE,
  buffer = 0.05,
  r_pen = 2,
  lambda_grid_mu = exp(seq(log(1e-04), log(10000), length.out = 40)),
  lambda_grid_sigma = exp(seq(log(1e-04), log(10000), length.out = 40)),
  method_mu = "BIC",
  method_sigma = "BIC",
  max_iterations = 50,
  tolerance = 1e-05
)
}
\arguments{
\item{x}{Numeric covariate vector.}

\item{y}{Numeric response vector of the same length as \code{x}.}

\item{m}{Integer difference order (penalty).}

\item{l}{Integer basis size/degree controller (as used in your implementation).}

\item{equi}{Logical; if TRUE, uses equally spaced knots, otherwise quantile-based.}

\item{tp}{Logical; thin-plate style option if implemented.}

\item{buffer}{Numeric boundary buffer beyond \eqn{[a,b]} for boundary knots.}

\item{r_pen}{Numeric or matrix; additional penalty specification if applicable.}

\item{lambda_grid_mu}{Numeric vector of candidate smoothing values for the mean.}

\item{lambda_grid_sigma}{Numeric vector of candidate smoothing values for the scale.}

\item{method_mu}{Character; selection criterion for \code{lambda_mu} (e.g., "GCV", "BIC").}

\item{method_sigma}{Character; selection criterion for \code{lambda_sigma}.}

\item{max_iterations}{Integer; maximum number of alternating iterations.}

\item{tolerance}{Numeric convergence tolerance for the alternating updates.}
}
\value{
An object of class \code{asp25psplines_fit} with elements such as
\code{mu_hat}, \code{sigma_hat}, \code{coef_mu}, \code{coef_sigma}, \code{lambda_mu}, \code{lambda_sigma},
and \code{diagnostics}.
}
\description{
Fits the conditional mean and log-scale by alternating penalised weighted
least squares. Smoothing parameters are selected per component; an optional
refinement step can calibrate interval coverage.
}
\examples{
\donttest{
set.seed(1)
x <- sort(runif(80)); y <- sin(2*pi*x) + rnorm(80, 0.25)
res <- run_all_funcs(x, y, m = 2, l = 10, equi = TRUE, tp = FALSE,
buffer = 0.05, lambda_grid_mu = NULL, lambda_grid_sigma = NULL,
method_mu = "GCV", method_sigma = "GCV", max_iterations = 20)
}
}
